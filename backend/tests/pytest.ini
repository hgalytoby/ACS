[pytest]
asyncio_mode=auto
log_cli=1
log_cli_level = DEBUG
log_cli_format = %(levelprefix)s | %(asctime)s | %(message)s (%(filename)s:%(lineno)s)
log_cli_date_format=%Y-%m-%d %H:%M:%S
addopts=-p no:warnings
env =
    MODE=TEST
